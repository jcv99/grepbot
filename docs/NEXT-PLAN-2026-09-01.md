# GrepBot — Next Plan (2026-09-01)

Consolidated action plan distilled from `docs/OPEN-PLAN.md`, re-checked
against `src/` @ v6.0.15-rc4-dev9 (post 6.x merge). `OPEN-PLAN.md` stays
the provenance source; this file is the working sequence.

## Step 0 — Re-verify plan against the 6.x merge (before any Phase 1 work)

`OPEN-PLAN.md` was generated at v5.10.9. The 6.x drop-in merged at
v6.0.15-rc4-dev7 diverged at v5.8.0, so every line reference below must be
re-confirmed before editing. Spot-check done 2026-09-01:

| Item | Status |
|---|---|
| 1.10 `Notification.requestPermission` catch | **CLOSED** — `src/ui.js:2939` already chains `.then(ok, ok)` with a rejection handler inside try/catch |
| 1.12 `banditTimer` split across collect.js/bandit.js | **CLOSED** — re-applied e1e8771 in v6.0.15-rc4-dev13 (merge had regressed it) |
| 1.1 captcha-hot / 1.2 `farmClaimsToday` rollover | **CLOSED** — dev19 (captcha-window ring + adaptive restore) / dev14 (rollover under claim lock) |
| Phase 1 items 1–9 | **ALL CLOSED** 2026-09-01, dev12–dev19. Item 1 (collect re-arm) verified intact from 05c3a46. Item 6 ported with a correction: v5.10.19 unlocked before the token gate, silently no-op'ing the breaker — dev18 records the sweep verdict BEFORE `gbUnlock`. |
| Manual in-game validation on `es146` | **OPEN, top blocker** — all coded work since v5.8.0 line waits on it |

## Phase 1 — Cheap correctness fixes (one commit each, smallest first)

**DONE 2026-09-01** — all 9 items landed as dev12–dev19 (item 1 was already
fixed at 05c3a46 and survived the merge). The 6.x merge had regressed
every other v5-line fix; each was re-ported with its original commit
referenced in the message.

| # | Item | Where | Fix sketch |
|---|---|---|---|
| 1 | `collect.js` lock-release → re-arm race | `collect.js` `scheduleCollectBg` | defer re-arm until after `gbUnlock` |
| 2 | `dump.js` ledger probe mutation | `dump.js` `pickDumpDestination` | `structuredClone` before probe |
| 3 | `banditTimer` cross-module split | `collect.js:232` → `bandit.js` | move declaration; wire `banditClearLoop` into `__grepbotDispose` |
| 4 | `farmClaimsToday` day-rollover race | `farms.js:1138-1147` | hold rollover inside `claim` lock, single `saveSoon` |
| 5 | captcha-hot filter sticky for day | `farms.js:1129` `farmCaptchaHot` | scope to TTL window, not "any claim today" |
| 6 | `farmScrapeRevive` race on lock expiry | `farms.js` | write token into "scrape in flight" marker breaker consults |
| 7 | `farmLoyaltyResearched` hidden write | `farms.js` | split read (`farmLoyaltyResearched`) from learn (`farmLoyaltyLearnTech`) |
| 8 | unify loyalty value in profit/duration pick | `farms.js` | one value (1.0 or 0.5/1.0) everywhere |
| 9 | `farmClaimPending` memo key missing lock | `farms.js` | include `gbLocked('claim')` in memo key |

Per item: `src/` edit → `@version` bump → `python3 build.py` → headless
smoke (`node .claude/skills/run-grepbot/driver.mjs`) → commit + tag.

## Phase 2 — Duplication (DEDUP PR order)

| # | Item | Risk |
|---|---|---|
| 2.1 | `docs/DEDUP_MAP.md` inventory (0 LOC) | none — do first |
| 2.2 | `gbButton` + `gbEmptyState` in `core.js`; queue-center action rows | low |
| 2.3 | `cfgBindBool` / `cfgBindNumber` in `ui.js`; migrate safe groups only (see exclusion list below) | low — **DONE v6.0.20** (11 exact-shape sites: diagnostics, intel-prefs, notify, UI, farm prefs; clamp/transform/nested/multi-key stay custom) |
| 2.4 | Queue Center presentation merge | low — **DONE v6.0.21** (`queueCenterLiveCard` / `queueCenterPlanCard` / `queueCenterFifoSection` collapse the 3-lane live-card + plan-card + FIFO-row repeats in `queue-center.js`; reconcile/FIFO ownership untouched) |
| 2.5 | Queue state utilities (lane-invariant) | medium — full in-game smoke |
| 2.6 | attack.js ↔ reinforce.js wave-planner merge | **HIGH — own session** |

**2.7 DONE v6.0.22** — `learnTemplate(stateKey, storeKey, j, opts)` in
`parse-inline.js` collapsed the 8 bridge-payload learn sites (spy, claim,
accept-units, attack, cancel, wonder-favor, hero, support). Left custom:
collect/farm URL-action strings, phoenician controller-shape +
`amountKey`, quest reward walker — different shapes, forced fit rejected.
Side effects (`farmLearnFromClaim`, `attackRememberTarget`) stay at call
sites. `supportTpl` gains the uniform `town_id` field (inert).

Do-not-migrate for 2.3: HIGH-RISK toggles (recruit, dodge auto, favor,
god spells, support send, dump, emergency cave, attack, reinforce,
spy-send, trade, rural/phoenician trade), scheduler-waking controls
(`ibAuto`, `abAuto`, `autoFarm`, `autoBandit`, `autoCollect`,
`autoResearch`, `autoCave`, `autoRuralTrade`, `autoRuralLevel`,
`autoCulture`, `autoDump`, `autoTrade`, `autoPtTrade`, `autoHero`,
`autoWallRepair`), controls with migrations or multi-key legacy reads.

## Phase 3 — Storage world-scoping

| # | Item | Gate |
|---|---|---|
| 3.1 | build.py gate: every `STORE.X` in exactly one of `WORLD_SCOPED_BASES` / `CROSS_WORLD_KEYS` | safe |
| 3.2 | classify ~114 unscoped keys (verify `state.tplHealth` already scoped first) | safe |
| 3.3 | `CONFIG_VER 13` migration with legacy read-fallback | **USER SIGN-OFF REQUIRED** — rewrites user config |

## Phase 4 — Performance (only after Phase 1 closes)

- 4.1 gate boot intervals on `state.auto*` flags (keep `renderTimers`,
  `updateStatus`, lock sweep unconditional; never move econ off `orchTick`)
- 4.2 cache `queueCenterTownIds` by `{count}:{lastFetchAt}` signature
- 4.3 short-circuit `updateStatus` on unchanged inputs
- 4.4 `state.seen` prune → insertion-order `Map` eviction

## Phase 5 — Structural (behind user go-ahead)

- 5.1 `STATE_SCHEMA` single source of truth (STORE + state + scoping)
- 5.2 `releaseLocks` → `gbOnDispose(fn)` registry
- 5.3 `txRun` split: `txPrepare` / `txDispatch` / `txReconcileLoop`
- 5.4 tx feature registry (`txBatch`/`txOnce`/`txOnCaptcha`/`txLogErr`)
- 5.5 `townEcon(townId)` cached superset reader
- 5.6 loop registry `{name, ms, fn, enabledIf}` for all `gbInterval`s

## Phase 6 — Observability / UX (opportunistic)

- 6.1 `BroadcastChannel('grepbot:events')` cross-tab captcha/panic/decisions
- 6.2 `AbortController` per `gbXhr` + `gbAbortFeature(feature)`
- 6.3 `IntersectionObserver` for off-screen widgets
- 6.7 `selectorHealthOk(selector)` + auto-recovery banner
- 6.9 game-version fingerprint toast (`Game.js_build_number`)

## Phase 7 — Build pipeline (needs user OK — CLAUDE.md no-tooling rule)

- 7.9 extend smoke driver: zero console errors, every tab renders, QC
  opens, Preflight 0 FAIL — **no user gate needed, do early**
- 7.2/7.3 jscpd + minimal ESLint (dev-only first) — gated on user OK
- 7.7 build.py TDZ / const-order check — pure Python, propose with 7.2
- 7.10 `docs/METRICS.md` baseline

## Phase 8 — Bug-hunt audit (re-run at start of every phase)

- `plannerEffect` `+a.wood || 0` coercion next to hard rule #2
- silent `catch (_){}` in write-adjacent paths
- `farmSnapDuration` 20% tolerance vs `FARM_DURATIONS`
- `caveCapacityFromLevel` hardcode vs live `GameDataBuildings` (es146)
- `parseTimerMinutes` min/h-only shapes vs es market strings
- `updateStatus` tooltip build while `document.hidden`

## Open questions (block the gated items)

1. Phase 3.3 world-scope migration — proceed? (only item that can lose
   user settings)
2. Phase 7.2/7.3/7.6 build-time jscpd/ESLint — acceptable given
   no-tooling rule? (Python-only in build.py, no npm dep at install)
3. Phase 2.6 wave-planner merge — own session now, or hold until
   attack.js + reinforce.js validated in-game?
4. In-game validation on `es146` — schedule? Everything since the 6.x
   merge is coded-but-unvalidated.
