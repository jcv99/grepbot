# Regression notes and helper archaeology

Moved out of `CLAUDE.md` so always-loaded file stay small. Nothing here optional when touch subsystem it names — every entry bug that shipped once. `CLAUDE.md` § *Hard rules* carry one-line version of each invariant; this file carry reason.

- **v5.10.61 farm reconcile blind + tombstoned villages could never re-post**: two holes kept farms starved after the v5.10.60 fix unblocked the chain. (1) `txReconcileNow`'s farm branch required `lootableAt` on BOTH snapshot and live read, but the game omits `lootable_at` on a claimable village — so every farm reconcile was permanently `'unknown'`, and each cadence re-stamped `unknownAt`, keeping the tombstone forever young (never aging into manual-review). Farm verdict now reads CURRENT claimability (`farmIsLootable` on the relation, which carries the null-means-lootable convention): on cooldown = `applied`, claimable = `unchanged`. (2) The dup path had no retry outcome at all: `applied` committed, `unchanged`/`unknown` re-blocked — a village with an old tombstone could NEVER post again, reconciling to `unchanged` every cadence forever (es146 2026-08-26: `unknownx35 okx27` every batch, 35/66 villages starved). Farm `unchanged` is PROOF the old claim did not land (village claimable now), so the dup path now drops the tombstone and re-enters `txRun` as a fresh attempt — the retry the hard rule forbids is the blind one, this one carries evidence. Farm-only carve-out; irreversible writes keep the block. Inconclusive reconciles also keep their ORIGINAL `unknownAt` now so a permanently blind entry still ages into manual-review instead of being re-armed forever.
- **v5.10.60 dup-reconcile `'unknown'` wedged intents + killed the farm batch**: `txRun`'s duplicate path re-reconciles an old `unknown` entry; the callback handled `applied` and `unchanged` but not `txReconcile`'s third verdict `unknown` (blind model read). Result: the entry stayed `reconciling` forever (runtime `txPrune` never expired in-flight states — only `txLoadNormalize` at reload did), every later attempt dup-blocked on it, and the skipped `onDone` silently killed the caller's callback chain. Observed on es146 (2026-08-26 log): a farm claim batch died on the first poisoned village (`farm:7169:716`), the `claim` lock leaked until its 22min TTL, and every later batch re-died on the next poisoned tombstone — island 64883 went unfarmed all day while the log only showed `skipped (in flight)`. Fix: the dup-reconcile callback now re-stamps `unknown` (recheck window re-arms, next cadence re-evaluates, same as the `unchanged` branch) and always fires `onDone`; `txPrune` gained a runtime in-flight expiry (`TX_INFLIGHT_MAX_MS` = 10min, an order of magnitude past the worst legit send+reconcile window) that demotes wedged `planned/precheck/sending/confirming/reconciling` to `unknown` with a log line. Rule: a callback that enumerates verdicts must exhaust them — a missing branch on a settle path is a deadlock, not a no-op.
- **v5.10.59 build-queue real-queue match**: timeout_unknown / pending from `bridgePost('build')` set the head to `manualReview` via `nativeFail` even when the order actually landed in the real queue (timer running, model not yet bumped). The 120s stuck-inflight check did not re-clear it because `inflight` was already null; the only exit was waiting for the real queue timer to finish or `×`-removing the row — the latter breaks Teatro prereq chains the user wants to keep. `nativeQueueReconcileBuild` now reads `abQueueInfo` (already exposed: `building_type`, `building_time`, `to_be_completed_at`, `tear_down`) and, when `j.manualReview + j.reconcile` is set and the real queue has a non-tear-down slot of the same `building_type` while `levels[building] < targetLevel`, clears `manualReview` and stamps `accepted:true` on a fresh inflight. The level-reached branch above still owns the `levels>=target` clear, and the 120s re-trip is now guarded on `!j.inflight.accepted` so an accepted job stays accepted until the timer completes. Real-queue-match detection only fires while the lane is `unknown`; an honest stuck post (real queue has nothing matching) still surfaces the manualReview prompt.
- **v5.10.58 recruit waiting-slot single authority + server-full backoff**: `dec6c90` shipped both sides of a contradiction — `recruitRefreshWaitingSlotJobs` re-marked a head `waiting-slot` on client-says-full while `nativeQueueRecruitHead` flipped it back `pending` and posted in the same scan (label flapped, re-mark dead). Refresh now only CLEARS stale `waiting-slot`; the send path owns the wake. When the client said full AND the server rejects, the head gets `slotRetryAt` (+5min, honored by `nativeQueueRecruitHead`) — before this, a genuinely full lane ate a rejection every cadence: budget slot + decision-memory strike per tick until the 5/15/60min skip window opened. Same commit: planner recruit cost lines via `gbNum` (a null GameData cost used to coerce onto 0 = unit marked free).
- **v5.10.57 farm claim verify after uncertain outcomes**: `finishClaimBatch` counted only confirmed bridge-ok as posted; a batch settling `timeout_unknown`/`pending` skipped `verifyClaims`, kept stale `lootable_at`, and re-posted into non-lootable villages next cadence. Uncertain outcomes now count as posted for verify. Also: farm option reads bounded to 1..4 (`+false`/`+''` coerce to option 0, which is not a claim); `state.nextFarmClaim` via `gbNum`; `farmClaimTiming` dropped the ms-epoch `nextClaimAt` beside its game-seconds fields (zero consumers). The provisional `{600:2}` option-map default stays DELIBERATE — an empty map short-circuits every resource claim as `farm-opt` with no post (the v5.10.50 deadlock); the learner corrects a wrong guess on the first verified claim.
- **v5.10.56 Registro display cap**: v5.10.53 dropped the 200-line ring but flush re-stringified the whole session buffer every rAF while the pane was open (O(n) per frame, n climbing all session), and the hidden-tab 250ms timer kept doing it in background tabs. Pane now renders a 2000-line tail slice (`gbLogDump`/Copiar still full), `renderLog` does not queue while `document.hidden`, and the boot `visibilitychange` repaint calls `renderLog`.
- **v5.10.55 rural parity gbNum + room clamp**: the parity deficit pick used `+st[r] || 0`, and `townResState` could ship NaN for a single resource on a partial client read — `NaN || 0` made an UNREAD resource always win as "lowest" (gbNum hard rule, third instance of the audit pattern). `townResState` now coerces per-resource with `gbNum`. Trade amount is clamped by warehouse room for the deficit resource (amount × farm ratio reserved per job) — the v1.5.4 haul-evaporates class. Unreadable warehouse logs once (`gbLogT`) instead of skipping silently; `minRatio` read-side clamped ≥ 1 against imported configs.
- **v5.10.54 rural-trade parity mode**: `ruralTradeScan` now picks the lowest of wood/stone/iron per town (skipping any already near-cap) and trades farms on the same island that offer that resource with `current_trade_ratio ≥ state.ruralTradeRatio` (default `1.1`, was `1.0`). Multiple farms offering the same resource are sorted by ratio desc with ties broken by relation id, so the best trade always wins inside a cycle. The legacy `state.ruralTradeRes` selector and its `data-cfg="rural-res"` UI are gone; `STORE.RURAL_TRADE_RES` stays in storage so old `localStorage` loads cleanly. The Grepolis `FarmTownPlayerRelation/trade` endpoint only accepts `{farm_town_id, amount}` — the backend derives the warehouse deduction from `farm.resource_offer` + `current_trade_ratio`, so the bot's only lever is the farm filter + trade size. Log line gained the target resource (`res wood|stone|iron`).

Ordered newest-ish first inside each section, not strictly chronological.

---

## 1. Encoding

- **v2.9.1 mojibake**: panel rendered `EconomÃ­a` / `ConstrucciÃ³n` / `Â·` / `â€¦` in-game while `src/` **and** artifact both valid UTF-8. Nothing double-encoded — browser decoded injected script as Latin-1, because neither install path carry charset (TM editor paste, or drag `file://` .user.js onto tab; `.user.js` no BOM, no `Content-Type`). Do not "fix" by re-encoding `src/`, adding BOM, or set `document.charset` — artifact now pure ASCII (build gate 4), so no multi-byte sequences left for any charset guess to corrupt. Verified: built file decodes byte-identically as UTF-8 and as Latin-1.

## 2. Preconditions and blind reads

- **v5.10.2 `+x` + `isFinite` read unreadable as `0`**: `gbProbeNum` coerced the getter result with `+` and then tested `isFinite`, so a client getter answering `null`, `''`, `[]` or `false` returned a perfectly finite `0` that every caller treated as a real reading — pop 0, warehouse capacity 0, "academy level 0" — and a guard then blocked on a value it had never read. Its own sibling `gbProbeAttr` already guarded `a[n] != null` first, which is what made the asymmetry visible. Same idiom sat in `gbBuildingLevel`, `researchConstant`, `researchResMod`, the academy/library level probes, the cave hide-capacity read and the culture stock read. All of them now go through `gbNum`, and there is no bare `isFinite(` left in `src/`. Rule: `+` is not a readability test.

- **v5.10.2 `gbSafe` was an unescaped sink named "safe"**: the sanitizing half of the `innerHTML` pair fell back to `return gbLit(html)` — raw pass-through — whenever `typeof DOMPurify === 'undefined'`, which is exactly the case its CDN `@require` fails to prevent. It had zero call sites in the whole repo, so the only thing the `@require` bought was a third-party fetch from cdnjs on every game page load. Removed both. A future feature that needs markup from the wire adds its own sanitizer deliberately instead of inheriting a dead one that is only safe when the network cooperates.

- **v5.9.9 random fallback never posted**: `abRandomPick` only ran when `abPickNextFromLevels` returned null, then handed its pick to `abBuildUp` — whose `abFinalValidate` re-ran `abPickNextFromLevels` and answered `no-valid-build` every single time. Whole feature shipped dead in v5.9.8. Worse, the failure was invisible: the catch-all called `noteBlocked`, which early-returns when the native lane is empty — always true on this path. Fix: `abFinalValidate` accepts an explicit `plan.randomFallback` pick (still re-validating max level, prerequisites, affordability, dedup and queue gates against the live model), and the fallback logs through its own `gbLogT` instead of `noteBlocked`. Rule: when a caller reaches a validator only in the state that validator treats as fatal, the validator needs an explicit-input branch, not a second opinion.

- **v1.5.4 preconditions**: gaps closed were **research** (post techs with no resource / research-point check — every scan burned budget slot on guaranteed rejection), **merchant** (buy without read gold balance), **rural trade** (no per-relation trade cooldown check, and it traded `wantRes` into town whose `wantRes` stock already at capacity — haul evaporates on arrival; dead `if (!blocked && !state.autoRuralTrade)` line it replaced could never be true, `autoRuralTrade` asserted at top of scan), and **militia/dodge** (`request_militia` with no farm / militia already standing / no free population, and `dodgeSafeTown` dodging *into* town that itself had incoming — one lost town become two). Do not "simplify" any of these into unconditional post; server rejection not free, cost request budget slot and decision-memory strike every cadence.
- **v1.5.3 audit**: free instant require model gold===0 + `gameNow()`; never `buyInstant` fallback on timeout; olympic need `allowPremiumCulture`+budget; quests `every()`+no DOM fallback; dodge hostile-only; attack town targets + overdue refuse; favor `temple_plunder`; recruit by unit building; host-gated scrapes/reports; claim/sleep reconcile; `gbXhr` budget+captcha skip.

## 3. Scheduling and locks

- **v5.10.2 orchestrator invented two feature keys**: `ORCH_CAPTCHA` and `ORCH_JRN` translate an orchestrator key into the key the POST layer actually uses, and both are the `feature` argument `bridgePost`/`gameAjaxPost` was called with — never the orchestrator's own key, never a lock name. Two entries were invented and matched nothing: `villrecruit: 'villageRecruit'` (recruit.js posts and gates on `'villrecruit'`) and `godspell: 'godspell'` (every cast goes out through `spellCastPost` as `'spell'`). Both failed silently in the same two ways: `captchaPaused()` on a key no post ever trips is permanently `false`, and `orchJrnOkSince` counted zero `ok` rows forever, so the adaptive backoff read a busy feature as idle and widened its cadence to the 8× ceiling. `god-spells.js` had the same mistake at the feature level — it gated on its LOCK name `'godspell'`, so a captcha on a cast never backed the scan off at all. Rule: a captcha key and a journal `f` are whatever was handed to `bridgePost`; a lock name is not a feature key.

- **v5.9.9 guard order**: `txActionGate` checked captcha, the breaker and decision memory *before* dry run, and charged the request budget before the planner ever ran. Dry run is documented as first for a reason — a captcha-paused or budget-starved feature is exactly when you want to inspect the payload it would send, and dry run sends nothing, so no downstream guard has anything left to protect. Restored order for writes: dry -> breaker -> safe -> health -> dedup -> planner -> budget -> captcha, with captcha re-read immediately before the send (a ladder that opened mid-tick still stops it; one that expired does not cost the slot the planner holds). Cost: one `aborted` tx record per attempt while a ladder is open, pruned at `TX_TERMINAL_TTL`, and `aborted` never blocks the next attempt.

- **v1.1.0**: `orchTick` sole econ scheduler (cadence-aware). Do not re-add per-feature `gbInterval` for cave/culture/trade/farm/ab/… alongside orch — that was thrash bug.
- **v1.4.0 lock registry**: never reintroduce module-local `*InFlight` boolean. Boolean can only be cleared by path that set it, so callback that never fires strand feature until reload, and `beforeunload` reset block did not run on bfcache/mobile exits. Use `gbLock(name)` / `gbUnlock(name)` / `gbLocked(name)` and add TTL entry to `GB_LOCK_TTL` for new features.
- **v1.4.0 orch**: one-feature-per-tick was throughput ceiling (3 actions/min) AND starvation bug (strict list order). Keep per-tick budget + overdue tie break; do not "simplify" back to single `return` after first match.
- **v1.4.0 adaptive cadence** infer "did feature act" from journal entry count for that feature, sampled 4s after run — feature scans return void, so no return value to trust.
- **v1.6 instant arming**: armed timer *accelerator*. Never delete 10s `gbInterval` or click hook in favour of it — clamped background timer can fire minutes late, and interval what recover that.
- **Farm/town scrape deadlines**: `nextFarmScrape` / `nextTownsScrape` added v0.3.2, **persisted to storage in v0.4.1** so cadence survive reloads. Stamp deadline at START of every scrape (prevents double-fire when manual `Farms now` / `Towns now` zero them); manual buttons zero deadline + call `farmTick()`, never arm own timer. Never use chained `setTimeout(scrape, 5min)` — background tabs throttle it to ~20min.
- **`scrapeAllFarms` empty-list early-return bug** (fixed v0.3.1): next deadline MUST be stamped even when `state.farmsParsed.length === 0` (stamp happens before fetch queue since v0.4.1).
- **Parallel farm fetch bursts** (N farms × 6 endpoint guesses) got throttled by game server — only first farm updated (v0.4.1: sequential queue + cached `farmAction`).
- **v1.3.5**: log/reqBudget use head-index rings (no `shift()` thrash); bandit wakes off `banditIdleUntil` instead of fixed 1.5s interval; DOM MutationObserver scope to `#ui_box`/windows/quests; CSRF hit poll 120s + force-hunt on captcha/auth errors.

## 4. Transport, ack and templates

- **v5.9.9 `cave-emergency` took the READ path**: `emergency.js` posts `caveStoreIron` under its own feature key so its captcha ladder and circuit stay separate from routine stashing — but the key was never added to `TX_WRITE_FEATURES`, so `txRun` skipped dry run, the circuit breaker, safe mode, template health, tx dedup, the planner and the action budget on an irreversible resource move. This is the exact failure mode the `airaw` comment in `planner.js` warns about; a new feature key is a new write registration, always.
- **v5.9.9 dodge borrowed `attackTpl`**: `dodgeSendOut` posts `arguments.type === 'support'` but read `state.attackTpl.action_name`. Templates key to the PAYLOAD, not the feature: dodging was coupled to whether the player had ever hand-sent an *attack*, and on a world where the support and attack action names differ it would have replayed the wrong one. Now reads `supportTpl` with the same `'sendUnits'` fallback. `favor.js` keeps `attackTpl` on purpose — temple plunder really is an attack payload.

- **v1.6.1 gpAjax callback contract** (was: *everything* logged `err timeout` while action actually executed). Read `GPAjax` in `archive/captures/grepo-dump/js/game.min.js`: `_ajax` wrap **bare function** callback as SUCCESS-ONLY handler (`u=function(e,i,o,r){if("function"==typeof a)a(i,r)}`), and success branch skipped whole when response lack `json`/`_srvtime`. So server-side rejection (`json.error` → HumanMessage) and empty bridge payload both leave caller hanging until `BRIDGE_TIMEOUT_MS`. Two consequences: (1) bare callback args `(data, t_token)`, **not** `(wnd, data)` — window handle only passed to `{success,error}` object form; (2) every post now also register `gbAjaxWatch(sig, settle)` and XHR spy settle it from raw response on `loadend` (`gbAjaxClaim` match by `model_url|action_name` for bridge, `controller/action` otherwise). Do not drop watcher and go back to trust gpAjax callback, and do not drop callback either — whichever fire first win, `settled` dedupe. `captcha_required === true` real captcha flag game itself keys on.
- **v1.6.3 template health** charged per **learned payload**, never per feature key. Feature `build` carry two different posts: auto-queue `buildUp` (hardcoded payload — no template can be stale) and instant complete (only user of `ibAction`/`ibActionR`). Because both journaled under `build`, five ordinary auto-queue server rejections invalidated `ibAction`, and `tpl-stale` gate in `bridgePost` then blocked *both* features until hand-click. v1.6.1 lit it: those rejections used to settle as `timeout`, which `jrnHard` exclude. `tplNameFor(feature, payload)` now resolve by `action_name`/`model_url`; add new learned template to that resolver, not just to `TPL_FEATURE_MAP`. `tplHealthOk` also treat unlearned template as healthy (caller fall back to constant) and expire invalidation after 30min — gate block only post that could ever record `ok`, so without expiry one bad streak permanent.
- **v1.4.0 dry run**: `state.dryRun` short-circuit inside `bridgePost` / `gameAjaxPost` only. Any new transport must route through those two or dry run silently stop covering it.
- **v1.1.0 journal scoping**: decision journal keys world-scoped (`wkey`) — town/village id mean nothing on other world, so unscoped journal would skip valid targets after world switch. Skip windows must ignore `skip:*` results: night pause fire thousands of times and would otherwise look like dead endpoint. `renderLog` also bail when `.log-list` `hidden` (Decisions sub-view active).
- **v1.6 phoenician**: offer parser (`ptParseOffers`) written against server-rendered markup **without live sample** — treat parse miss as unknown (post nothing), and confirm with Config → *Copy offer HTML* before trust bulk trade. Never let scraped stock be only bound on bulk amount.

## 5. Rendering

- **v1.4.0 keyed rows**: `renderFarms` / `renderWorld` / `renderAttack` patch keyed rows (`tr[data-key]`, `div[data-town]`) and rebuild only when id **set** change — compare membership, not order, or user column-sort wipe on every repaint. `renderAttack` no longer interpolate town names into `innerHTML`.
- **v4.48.0 `gbPaint(host, build, {key})`** (core) generalise that convention to every window. `build(stage)` fill **detached** node; identical structure then patched in place (text data + attributes, plus `value`/`checked` on any control that **not** `document.activeElement`), so live node keep listeners, scroll offset, `:hover` and whatever user was typing. Never move/insert/delete nodes — structural difference fall back to wholesale replace. `key` identity of what handlers close over (job ids, town id, row order); when change subtree replaced instead of patched, so kept button can never fire for job that moved or left. Users: Queue Center body, `nativeRenderQueuePanel`, `renderAbQueue`, and every `gbWidgetRegister` widget (`o.key` optional). Do not replace `gbPaint` call with bare `replaceChildren()` loop on timer — that bug it exist to prevent (Build tab's target inputs wipe mid-typing by any queue mutation, and 1s HUD countdown rebuilt whole window every tick).
- **Queue Center render contract (v4.47.0)**: `renderQueueCenter()` *request* (dirty flag + 90ms coalesce) — sweep that touch 20 jobs paint once, not 20 times. `renderQueueCenterFlush()` click path: paint now and skip throttled `nativeQueueReconcileTown` because mutation already reconciled lane. Every paint render into detached node and swap only when `queueCenterSig()` (innerHTML with `.gb-qc-eta` cells blanked) differ; on skipped swap fresh eta baselines copied onto live cells. Real-queue clocks moved by 1s ticker that only write text into `.gb-qc-eta` (`data-eta` seconds + `data-t0` stamp) and ask for one repaint when row reach 0. Background paints defer while `<select>` inside window hold focus.

## 6. Parsers and bridge discovery

- **v0.4.2**: farm discovery + claims + bandit moved to game bridge (`MM`/`ITowns`/`gpAjax`) copied from ModernBot (github.com/Sau1707/ModernBot, `src/autoBootcamp.js` + `src/autoFarm.js`). DOM-only bandit clicking never worked unless camp window open and attackable — model path need no window.
- **v0.4.3**: objects passed INTO page functions must go through `cloneInto` in content-script contexts; userscript sandbox only need `unsafeWindow`. XHR spy sniff request BODIES on `frontend_bridge` (`sniffBridgeBody`) — manual Recoger click teach exact claim payload (`claimTpl`, persisted) that `claimFarm` mimic. `verifyClaims` re-read relations 10s after fire and log whether `lootable_at` flip. `farmsFromGame` try both `getOnlyCollectionByName` and `getCollections().FarmTownPlayerRelation[0]`, log relation status distribution once per session.
- **`parseFarms`** accept `12345 500 600 | notes` (bare id, no leading pipe) since v0.4.1; pipe format `id | x y | eta | notes` still canonical.
- **Auto-collect regex must be anchored**: `/^\d{1,2}\s*min$/` — hour strings (`1h 30min`, `4h`) won't match, so they skip automatically.
- **v0.5.0 (merge)**: standalone `grepInstantBuild.js` (`@grant none`, page context) ported into Build tab. Two port gotchas: (1) userscript sandboxed, so `GPWindowMgr`/`gpAjax` MUST be reached via `gameUw()`, never `window.`; (2) auto-complete need in-flight guard (now `gbLock`) or completion wave longer than 10s scan interval re-fire and double-post. `fetchReport` stamp `state.seen` only after successful parse — stamp on any HTTP response lost reports to transient error pages.

## 7. Forks and dead branches

- **v2.7.0** merged out-of-tree `GrepBot-2.5.9.user.js` "híbrido" fork. That file forked well before 2.5.9 (94 of 639 shared functions differed from repo's own v2.5.9 build), so only two things came across: Queue Center window and `jrnTransientDynamicResult`. Everything else fork carried **older**, and re-porting would undo named fix — its `nativeQueueAdd*` refused appends while head in flight (undone v2.5.4), its `nativeQueueReconcileBuild` only reconciled `inflight.accepted` (leave un-acknowledged post stranded), and its `nativeUiScan` picked one host tile per building by visibility score instead of v2.5.5 body-level panel mount. Fork also predates `gbAjaxWatch`, `captcha_required`, phoenician trade and evidence system. Do not merge from it again without re-run per-function diff.
- **Retired surfaces**: WebExtension build (`web-ext/`, `grepbot.xpi`) and standalone `grepInstantBuild.js` retired v0.5.0. Local relay (`src/relay.js`, `tools/relay-mcp/`, `.mcp.json`) and its two header pills removed v5.2.2; only `airaw` write-feature + lock TTL entries survive, deliberately, so future raw-payload caller inherit guards. Any memory note describe `.xpi` rebuild or relay MCP stale.

---

## 8. Better-practice helpers (v4.62–v4.65 sweep) — full notes

Platform primitives, no new dependencies. `CLAUDE.md` list names; detail here.

- **`structuredClone(value)`** — prefer over `JSON.parse(JSON.stringify(x))` for any config / state snapshot. Preserve `Date` / `Map` / `Set` / `RegExp` / `ArrayBuffer` / typed arrays / `Error` with `cause` / `stack`, handle circular references, throw `DataCloneError` instead of silently drop `undefined` / functions / `Symbol`s. Used in `qol.js` (`qolConfigSnapshot` / `qolSaveTemplate` / `qolApplyTemplate` / `qolImportConfig`).
- **`BOOT_TIMING`** (`src/boot.js`) — frozen module-scope object of every cadence boot block use (INBOX_SCRAPE_MS, FARM_TICK_MS, THRESHOLD_CHECK_MS, STATUS_UPDATE_MS, OVERVIEW_RENDER_MS, LOCK_SWEEP_MS, NATIVE_QUEUE_LOOP_MS, plus boot-step delays FARM_WAKE_MS / QUEST_SCAN_BOOT_MS / IB_SCAN_BOOT_MS / AB_TARGETS_MS / NATIVE_UI_SCAN_MS / NATIVE_QUEUE_BOOT_MS / ORCH_FIRST_TICK_MS / HUD_RESTORE_MS / IB_CLICK_HOOK_MS, plus FIRST_FARM_DEADLINE_MS / FIRST_TOWNS_DEADLINE_MS that pair with persisted `state.nextFarmScrape` / `state.nextTownsScrape` deadlines). Naked `30000` / `15000` / `10000` in `boot.js` regression.
- **`gbNum(raw)`** (`src/core.js`) — the only numeric read of a client value. Returns a finite number or `null`; rejects `null` / `undefined` / `''` / `' '` / booleans / arrays / objects instead of coercing them. `+raw` maps every one of those onto a finite `0`, so the old `const v = +getter(); if (isFinite(v))` idiom accepted an unreadable getter as a real reading. `gbProbeNum` / `gbProbeAttr` / `gbBuildingLevel` / `researchConstant` / `researchResMod` / cave hide capacity / culture stock all route through it.
- **`gbLit(html)`** (`src/core.js`) — the one named bucket for `innerHTML`: LITERAL HTML ONLY (pass-through; an audit pass greps `innerHTML = gbLit(...)` and confirms no `${…}` interpolation). Wire values go in as `textContent`. There is deliberately no sanitizing sibling — see § 2 for why `gbSafe` and its DOMPurify `@require` were removed in v5.10.2.
- **Listener bag `AbortController`** (`src/core.js`) — one per-instance `AbortController`; every `gbListen` call pass `{ signal: gbListenerSignal }` so dispose call `gbListenerAbort.abort()` and detach every listener in one call. Bag survive as debug mirror only.
- **`gbPaint` selection preservation** (`src/core.js`) — snapshot `document.activeElement.id` + `selectionStart` / `selectionEnd` / `selectionDirection` before structural replace; restore focus + caret in new tree via `CSS.escape(id)` + `setSelectionRange`. Patch path already safe via `activeElement` skip on `.value` / `.checked`; this guard rarer REPLACE path.
- **`renderLog` rAF coalesce** (`src/core.js`) — visible tab coalesce per frame via `requestAnimationFrame` so burst of log lines paint once, not once-per-line. Hidden tab keep 250ms `gbTimeout` (rAF paused).
- **`scheduleAutoCollect` rAF** (`src/collect.js`) — same pattern for `MutationObserver` → auto-collect path; visible tab coalesce per frame, hidden tab keep 800ms timer.
- **JSON.parse size cap** (`src/bridge.js`) — `gbAjaxUnwrap` cap `JSON.parse` input at `GB_AJSON_PARSE_MAX = 256 KB`. `noteCaptchaBody` still sniff only first 4 KB upstream; this guard second line against oversized hostile body. Largest legitimate bridge payload today all-towns scrape at ~32 KB.
- **Captcha status pre-check** (`src/bridge.js`) — `gbAjaxWatch`'s settle callback classify status-first: 429 / 503 open server-pressure bus with `Retry-After` parsed to seconds; 200 with empty body short-circuit to `'soft-empty'` (skip class, not hard error, so three in row don't open `JRN_BACKOFF` skip window for nothing). `captcha_required === true` precedence preserved.
- **`saveSoon(key, val)` / `saveFlush()`** (`src/core.js`, v5.3.0) — 400ms coalesced `GM_setValue`. `state` stay live in memory; only write debounced, last value per key win. Village sweep was motivating case: `fetchFarmResources` write **whole** `state.farmResources` map and `towns.js` whole `state.townResources` map once **per village/town**, so 40-village account paid 40 full serializations per 5min cycle. `whyNote` had same shape (entire 200-row `whyLog` re-serialized per decision note). **Debounced write only safe because of flush**: `saveFlush()` called from `releaseLocks` (`boot.js`, on `pagehide`/`beforeunload`) as LAST step — after every other handler had chance to queue write — and from `__grepbotDispose` BEFORE `gbClearTimers()`, or pending timer die with its payload. `txSave` deliberately left synchronous: `committed` transaction must be durable immediately, since recovery path for lost write re-posting irreversible action.
- **`renderFarmsSoon` / `renderWorldSoon` / `checkThresholdsSoon`** (v5.3.0) — same fix on paint side of that cascade; `checkThresholds` full walk of `farmsParsed`, and call it per village made sweep O(N²) for answer that only depend on finished state.
- **Hidden-tab render guards** (v5.3.0) — `renderTimers` (1s) called `farmClaimTiming()`, which walk whole `FarmTownPlayerRelation` collection, against tab nobody looking at; `contextMenuScan` (750ms) ran `querySelectorAll` + `getBoundingClientRect` + `elementFromPoint`, three forced layout flushes 80×/min, likewise. All now bail on `document.hidden`, and `boot.js` repaint on `visibilitychange`/`pageshow` so first visible frame correct instead of frozen countdown. Context menu deliberately does NOT dispose while hidden — hidden tab not closed popup. **Note:** `renderFarms` / `renderWorld` also grew hidden-section guard, but their hosts (`.farms-list`, `.world-list`, `.world-totals`, `#gb-sleep-status`) **absent from panel markup** in current tab set, so both functions early-return on every call today. They left in place rather than deleted: restoring those tables product decision, not cleanup.
- **`SPY_LOAD_GATE_RE`** (`src/spy.js`, v5.3.0) — XHR `load` reader used to attach to **every** game request and, under 100KB, read `responseText` and run full `JSON.parse` plus recursive report walk. SPA issue these constantly. Listener now attach only when URL could plausibly carry report or quest reward. Bare `/index.php?...` deliberately NOT in gate — game's generic endpoint and match nearly every request, which exact cost being removed. Tradeoff: >100KB "report_id buried in unnamed payload" scavenger no longer run on those responses; named report URLs still caught on send path by `queueReport` / `queueReportList`.
- **Keyboard fingerprints** (`src/qol.js`, v5.3.0) — `gbKeyFingerprints` had `if (e.altKey) return []`, so **every** `Ctrl+Alt+…` binding unreachable, and `gbKeyChars` only emitted single printable characters, so any binding on named key (`Backspace`, `Escape`, …) could never match either. Both silent — binding simply never fired. Modifier order in binding string now fixed as `Ctrl+Alt+Shift+<key>`; Ctrl/Meta still REQUIRED so bare `Alt+key` still belong to game. New defaults: `Ctrl+Shift+Backspace` (panic — no confirm on way in on purpose, kill switch and itself reversible via `gbPanicRecover`), `Ctrl+Alt+P` (profiler), `Ctrl+Shift+B` (copy bundle).
- **`Error.cause` on swallowed catches** — bare `catch (_) {}` in `alerts.js` (webhook save paths, desktop notify, intel digest) and `boot.js` (boot ticks, `visibilitychange` / `pageshow` handlers, releaseLocks sequence) route to `gbLogT('silent-err:tag', 60_000, 'name: ' + String(e?.message || e).slice(0, 80))`. Surrounding tick keep running; cause land on Log tab.
- **Favor pool attribute names** (`src/favor.js`, `src/stats.js`, `src/recruit.js`, v5.8.4) — every favor read probed `fav[god]` / `fav['favor_' + god]` and, for cap, `max_favor_<god>` / `favor_<god>_max` / `max_<god>`. `PlayerGods` carry **none** of those names. From captured client bundle (`archive/captures/grepo-dump/js/game.min.js`, `GameModels.PlayerGods`): pool `<god>_favor` (model fire `change:<god>_favor`), cap single global `max_favor`, and `production_overview` `{ <god>: { current, production } }` with production in favor/hour. Symptoms: HUD block print `favor (dioses) no legible`, Preflight report `0/8 pozo(s) legibles, 0 con maximo legible`, and `recruitCanBuild` / `recruitAffordableAmount` refused every mythical unit because pool read came back `NaN`. Now ONE reader (`favorForGod` / `favorMaxPool` / `favorProdPerHour` / `favorGodsList` in `favor.js`) that every consumer call; legacy flat shape stay as trailing probes, never as primary. HUD also prefer client's own production number over measured rate, so ETA column exact on first paint instead of after two samples.
- **Farm scrape breaker could never revive from traffic** (`src/farms.js`, v5.8.4) — `learnFarmAction` returned early when observed action equalled stored one, and `farmScrapeRevive` call sat *after* that return. On world whose action had been learned in earlier session advertised recovery impossible: breaker trip, Preflight said "teach it by opening village", player opened one, identical action came back over wire, and breaker stay dead until Config toggle cycled. Evidence evidence whether or not string changed — revive now run on every observation.
- **`tx registry` counted tombstones as live posts** (`src/stats.js`, v5.8.4) — `inflight` regex matched `dryrun` and every terminal state (`aborted|failed|unknown|manual-review`) as well as moving ones, so registry full of tombstones reported `384 transactions, 213 live, 213 unknown`: same entries counted twice, under label claiming posts in flight. `live` now planned/precheck/sending/confirming/reconciling only, and `unknown` (still self-reconciling) reported apart from `manual-review` (waiting on human), in both `tx registry` and `guards` probes.
- **manual-review tombstones grew without bound** (`src/tx.js`, `src/planner.js`, v5.8.4) — nothing ever pruned `manual-review`, by design: blocking tombstone. But it also keep full reconciliation snapshot forever, and live account reached 213 of them. `txPrune` now strip snapshot down to `{kind, qid}` (all `txPrune` and `questClearReviewForTx` ever read back) when entry become tombstone, and `txCapReview` cap pile at `TX_REVIEW_MAX = 100`, drop oldest-first and **only** entries older than `TX_REVIEW_MIN_AGE_MS` (7 days), with `gbLog` line. Nothing younger can be dropped and nothing dropped silently.
- **`academy read path` probed wrong town** (`src/stats.js`, v5.8.4) — probe read `towns[0]`, but research queue per-town fragment client only fill for town on screen. On any account whose first town not open one probe reported `real queue UNREADABLE` and, through `researchPointsSpent`, `research points UNREADABLE` — permanent warning about read path that works. Now probe open town (`Game.townId`) when that town in list, and name which town it read.
- **Wall damage / hero stamina not in this client at all** (`src/build-auto.js`, `src/military.js`, `src/stats.js`, v5.8.4) — both readers written as "attribute name not captured yet". Captured bundle settle it: `GameModels.PlayerHero` declare level / experience_points / home_town_id / origin_town_* / target_town_* / cured_at / assignment_type / type and nothing else, and only `*damage*` identifiers in whole file battle-report unit counts. Probes stay (different world may ship different build) but Preflight now say client does not expose value instead of imply capture pending, and hero row only warn when `autoHero` ON.

- **v5.10.1 naval mythical (hydra) auto-recruit dead without `def.god`** (`src/core.js`, `src/recruit.js`) — `recruitCanBuild` and `recruitAffordableAmount` derive `requiredGod` from `GameData.units.<id>.god` and hard-block when missing. GameData builds that carry `is_mythical:true` + a non-zero `favor` cost but no `god` field (hydra on several worlds) therefore refused every auto-recruit even with Poseidon favor, pop and resources met in the harbor. `NAVAL_MYTHICAL_UNITS` already existed for the naval lane classifier; a sibling `MYTHICAL_UNIT_GOD = { hydra: 'poseidon' }` (helper `mythicalUnitGod(unitId)` in `core.js`) now fills the gap, with `def.god` still primary. Favor is irreversible, so an unknown god (unit not in the map AND `def.god` missing) still hard-blocks — same as today — instead of spending against a guess. Add a new naval mythical to both `NAVAL_MYTHICAL_UNITS` and `MYTHICAL_UNIT_GOD` in the same patch.
- **v5.10.43 recruit queue max unread → hydra stuck waiting-slot** (`src/recruit.js`, `src/native-ui.js`) — `recruitQueueInfo` probed `col.getMaxQueueLength` / `max_queue_length` / `GameDataUnitQueue.getQueueMax`, none of which exist in the captured client. `max` stayed null, so `recruitQueueHasSpace` only allowed a post when that lane was **empty**. Any ship already in the harbor froze a hydra (or bireme) FIFO head at `waiting-slot · cola real llena` despite free slots and full resources/favor. Live API is `GameDataConstructionQueue.getUnitOrdersQueueLength()` (hardcoded 7 per lane; full = 2× that across barracks+docks) — same source build/research already used. Also: lane filter now prefers `getOrders('docks'|'barracks')`; mythical god read accepts `god_id`; `nativeRecruitSplitTown` moves via `recruitIsNaval` so hydra without `is_naval` leaves the land lane.
- **v5.10.44 recruit queue unread reported as full** (`src/recruit.js`) — screenshot: docks UI `Cola de construcción (0/7)` empty, GrepBot hydra head stuck `waiting-slot · cola real llena`. Cause: `recruitQueueHasSpace` returned `false` when `known:false` (unreadable collection / probe throw), violating the unread→blind hard rule, and the models fallback kept id-less rows (`return true`), which could inflate `len` against an empty UI. Now: prefer `getCount`/`getOrders`/`getAllOrders` (same path as the docks header), drop id-less phantoms, and treat unread as space (server judges) with an explicit blind log; full reason shows `(len/max)`.
- **v5.10.45 stale waiting-slot + null-max compare** (`src/recruit.js`, `src/native-ui.js`) — after 5.10.44 paste, hydra could stay `waiting-slot · cola real llena` (no `N/7`) because orch never re-probed while farm-hold/early-return skipped the scan, leaving the pre-upgrade reason. Also `!(len < max)` with `max==null` is always true in JS (`len < null` → false), so a readable empty queue could still block. Now: prefer `getNavalUnitOrdersCount`/`getGroundUnitOrdersCount`, compare `len >= coercedMax`, clear stale `waiting-slot` in `nativeQueueRecruitHead` when space exists and wake `recruitScan`, reason text `puerto N/7`.
- **v5.10.47 client queue-full gate removed for native recruit** (`src/recruit.js`, `src/native-ui.js`) — v5.10.45 still showed exact pre-upgrade `waiting-slot · cola real llena` (string no longer written by recruit) while docks `(0/7)` empty: `recruitScan` refreshed nothing when `farmFirstHold`/early returns skipped the loop, and UI render never called `nativeQueueRecruitHead`. Client `getNavalUnitOrdersCount`/models length cannot be trusted against the docks header. Fix: refresh/clear waiting-slot before holds; never hard-block native FIFO (or `recruitValidateJob`) on client lane length — server is authority; status moves on to `waiting-resources` / post.
- **v5.10.48 farm-first starved native recruit FIFO** (`src/orchestrate.js`, `src/recruit.js`, `src/planner.js`) — after queue-full was cleared, hydra stayed `pending · 59 × 1` for minutes while docks UI showed buildable (exact discounted costs) and `(0/7)`. Cause: `orchFarmFirst` + `farmFirstHold` skipped all `recruit` dispatches whenever any village was lootable (`autoFarm` ON), so a player-armed FIFO never got a tick. Also afford/planner used raw `GameData` unit costs, missing `GeneralModifications.getUnitBuildResourcesModification` (mathematics/nereids/…), so exact UI stock looked short. Fix: native FIFO exempt from farm-first (legacy goals still held); scale wood/stone/iron by the live modification factor (force `is_naval` for hydra); planner matches.
- **v5.10.49 native FIFO stole farm/Recoger budget** (`src/orchestrate.js`, `src/recruit.js`) — v5.10.48 let hydra post through whole farm claim batches; overdue sort also put recruit ahead of due farm. Claims/Recoger hit `skip:budget`. Fix: when farm-first, farm always sorts before units; native FIFO still exempt but defers the post while `claim` lock held or action budget ≥75% / soft ceiling — UI stays `listo (espera aldeas)`.
- **v5.10.50 lootable villages not claimed / silent claim lock** (`src/farms.js`, `src/core.js`, `src/ui.js`) — claim batch took `claim` lock then every village `skip`/`tpl-stale`/`remembered` before send (`action=1/54`); 10s verify still held the lock so farm-first looked busy while villages stayed lootable. HTTP scrape `farm N: no data (no endpoint matched)` was misread as a claim failure. Empty stored `farmOptionMap` (`{}` from Forget-options) also dropped the `{300:1}` default. Fix: unlock immediately when 0 posts, always log `claim outcomes` + flash; prefix scrape lines `farm-scrape`; restore default 5min option on empty map.- **v5.10.51 claim skipxN with no post** (`src/farms.js`) — after 5.10.50, batches unlocked but still `skipx66`: resource claims only fell back to learned **5min**, so a map with only 10min+ (loyalty teach / hand claim) and `farmLongClaims` OFF wanted 300, found nothing, `done('skip')` every village. Fix: `farmOptionResolve` uses exact → longest learned ≤ want → shortest learned (never invents an index); skip reasons are `skip:opt` / `skip:wh` / `skip:no-town` / `skip:units-*` so outcomes are diagnosable; batch logs option map once.
- **v5.10.64 farm option map wrong forever: 10min want gathered 40min** (`src/farms.js`, `src/core.js`) — es146 log: every batch posted `map=10min=2` (the provisional default restored on empty map) and villages gathered exactly 2400s three cycles in a row; account has Booty (Botín), where options are 10/40/240/600min so option 2 = 40min (10min = option 1; base set 5/20/120/300 has no 10min at all, option 2 = 20min — the default was wrong both ways). Three guards all dead: the sniffer bails on self posts before `farmLearnFromClaim` (bot claims never taught, despite the "next hand/bot claim confirms" comment), `farmLoyaltyAutoTeachTick` early-returns because the provisional default makes `farmOptionFor(600)` non-null (GameData derive + teach banner suppressed), and `verifyClaims` only checked lootable_at *changed*, never the duration. Fix: batch records the posted option per village (`farmPostedOpts`); `verifyClaims` snaps each moved lootable_at to a duration and learns it via one shared `farmOptionMapLearn` (server state, not payload — cannot self-reinforce, P1-07 covers templates), warns once when gathered ≠ wanted; `farmOptionSetDerive` identifies the active set (base/Booty are disjoint) from one confirmed observation and fills all four indexes, derived entries re-confirm the same way; `FARM_DURATIONS` corrected to the union of both wiki sets plus legacy 90m/3h/8h snap targets (was missing 2h/5h/10h, carried fictitious-only entries); provisional default now `10min=1` — shortest offer in both sets, so a wrong seed never over-gathers and self-corrects after one batch.

- **v6.0.15-rc4-dev7 merge: 6.x drop-in reconciled into `src/`** (all modules) — the parallel 6.x line (`grepbot_v6.0.15-rc4-dev6.user.js`) diverged at v5.8.0 and never saw the v5.9–v5.10.64 fixes; this repo's line never saw 6.x (Telegram alerts, city designer + demolition planner, legacy-world migration, Web-Locks leader election, wake-scheduled farm claims, bigger Queue Center). Split via `archive/split_v6.py` (53 modules, concat reproduces the artifact byte-for-byte modulo 6 seam blank lines; build.py comment-strip + ASCII-escape verified against the normalized drop-in). The 6.x line's own improvements are kept, not reverted: per-building manual-review scoping (a tombstone blocks only its own building, not the lane), expected-reject classification (`gbExpectedServerReject`), shared-state reload across tabs instead of BroadcastChannel. Still missing fixes were ported forward:
- **gbNum discipline restored on the 6.x line** (`src/core.js`, `src/tx.js`, `src/bridge.js`, `src/build-tab.js`, `src/emergency.js`, `src/farms.js`, `src/recruit.js`) — 6.x's `txNum`/`gbProbeNum`/`gbProbeAttr` were the pre-v5.10.2 `+v`+`isFinite` idiom: `+null`/`+''` → finite `0`, so an unreadable getter read as a real zero. Worst live hole: `ibGoldCost` read `GameDataInstantBuy.getPriceForType` null → price `0` → "free instant" verdict on a paid order. `txNum` now delegates to `gbNum`; both probes keep the first gbNum-readable value; `recruitCostFieldKnown` no longer accepts a null cost field as known-zero (that path fed the planner's recruit reservation); `farmVillageLevel` and `emergency.js`'s warehouse reader route through gbNum.
- **real-queue match unstuck timeout_unknown build head** (`src/native-ui.js`, port of v5.10.59/40a470f) — 6.x's `nativeQueueReconcileBuild` was the pre-fix shape: a `timeout_unknown`/`pending` head sat at manualReview even when the order was visibly in the real queue. Now matches the reconcile snapshot against a non-tear-down `abQueueInfo` slot by building_type, stamps `accepted:true`, and the 120s stuck-inflight re-trip is guarded on `!accepted`.
- **slotRetryAt server-full backoff for native recruit FIFO** (`src/recruit.js`, `src/native-ui.js`, port of v5.10.58/792bcc9) — when the server rejects with queue-full (`gbExpectedServerReject` → `waiting-queue-full`) after the client said there was room, the head is stamped `slotRetryAt` (+5min) and `nativeQueueRecruitCandidates` honors it, stopping the every-cadence rejection that burned budget slots and stacked decision-memory strikes. 6.x never had the flapping refresh half of the bug (no `recruitRefreshWaitingSlotJobs`), so only the backoff half was needed.
- **unit-queue max probe: `GameDataConstructionQueue.getUnitOrdersQueueLength`** (`src/recruit.js`, port of v5.10.43) — the captured client's live per-lane unit-queue API was absent from 6.x's probe list (it only probed `GameDataUnitQueue`/`GameDataUnits` names that match nothing in the live bundle); without it, `max` depended on DOM scraping alone and a ship in the harbor could freeze a naval FIFO head at `waiting-slot`.
- **naval mythical maps: hydra leaves the land lane, favor gate finds its god** (`src/core.js`, `src/recruit.js`, `src/native-ui.js`, port of v5.10.1) — 6.x keyed naval class, controller choice and the mythical favor gate on `def.is_naval` / `def.god`, which some GameData builds omit for hydra: wrong controller (barracks), wrong lane (land), and a hard favor block with full Poseidon pool. `NAVAL_MYTHICAL_UNITS` + `MYTHICAL_UNIT_GOD` + `mythicalUnitGod` + `recruitIsNaval` restored as the single shared classifier; `nativeUnitIsNaval` delegates to it. The v5.10.48 modification-factor clamp (`GeneralModifications.getUnitBuildResourcesModification`) was NOT ported on purpose: 6.x never clamps affordability on raw GameData costs (unknown cost = blind, server judges), so the under-estimate that made hydra look unaffordable cannot arise.
- **gbSafe/DOMPurify dropped again** (`src/core.js`, `src/header.js`) — the 6.x drop-in reintroduced the v5.10.2-removed pattern: a `gbSafe` with zero call sites plus a CDN `@require` that falls back to raw pass-through when the CDN load fails. Same removal rationale applies; `gbLit` + `textContent` discipline stands.
- **v6.0.15-rc4-dev8: farm self-correct + tx dup-path fixes ported** (`src/farms.js`, `src/core.js`, `src/tx.js`, ports of v5.10.64 / c0593e6 / 679d8cb) — three gaps the dev7 merge missed, confirmed by the post-merge explorers: (1) the provisional option-map seed is now `{600:1}` — option 1 is the shortest offer in BOTH documented sets (5min base / 10min Booty), so a wrong seed under-gathers (safe) instead of the old `{300:1}` over-gathering 10min on a Booty world; (2) `verifyClaims` now runs `farmVerifyLearnMap`: each batch records the posted option per village (`farmPostedOpts`), and a village whose `lootable_at` moved teaches `map[snappedDuration] = postedOption` via `farmOptionMapLearn` + `farmOptionSetDerive` (base/Booty sets are disjoint, one observation pins all four indexes), warning once when gathered ≠ wanted — without this the map was only teachable by hand because `parse-inline.js` skips self posts (`if (!isSelfBridge(j)) farmLearnFromClaim(j)`), and the v5.10.64 10min=2→40min Booty bug could recur silently; `FARM_DURATIONS` restored to the full union (was missing 2h/5h/10h, so 40min snapped to its real 2400 instead of drifting); (3) `txFarmStatus` returns `lootableNow` via `farmIsLootable` (the game omits `lootable_at` on a claimable village, so the old null-vs-null compare made every farm reconcile permanently `unknown`), the farm reconcile verdict reads current claimability (on-cooldown = applied, claimable = unchanged), and the dup-path reconcile now: farm `unchanged` = proof the claim did NOT land → drop the tombstone and re-enter as a fresh attempt (es146 2026-08-26: 35/66 villages starved on the tombstone loop); non-farm `unchanged` = `unknown` + next-cadence re-evaluate instead of the 6.x 250ms blind re-post that could double-spend an irreversible write; `unknown` verdict = re-stamp + `onDone('unknown')` so a poisoned village can no longer wedge a claim batch and leak the claim lock until TTL.
- **Known divergences left on the 6.x side** (validate in-game on es146 before revisiting): 6.x keeps wake-scheduled farm claims (`farmScheduleClaimWake`) where v5.10.52 went fixed-cadence — its option-map learner has conflict dedupe + derive, which covers the 10min/40min Booty bug differently than v5.10.64's posted-option verify; unreadable unit queue is `queue-unknown`/skip (conservative) where v5.10.47 posts and lets the server judge — watch for a stuck naval FIFO on es146; orch deadlock detection (`orchDeadlock*`) and trade-deadlock drain exist only in the v5 line, 6.x answers with overflow trading (`tradeOverflow*`); BroadcastChannel captcha escalation (v5.10.36) is replaced by Web Locks + shared-state reload.
