# Reverse-engineering captures: saved game pages, HAR, grepo-dump.
# ~16M, includes two copies of the 4MB minified game.min.js. Local refs only.
archive/captures/

# Local dumps: findings JSON exported from the panel, smoke runs.
archive/data/

# Headless smoke output (run-grepbot skill): screenshots, DOM dumps, preflight.
data/smoke/

# Build gate bookkeeping (src hash + last built version). Machine-local state.
.build-stamp.json

# Claude Code machine-local overrides
.claude/settings.local.json

# Kimi Code project-local overrides (paths, may include machine-specific dirs)
.kimi-code/

# OS
.DS_Store
Thumbs.db
grepbot.user.js.prod
