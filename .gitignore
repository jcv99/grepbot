# Whitelist mode: GitHub carries only the buildable source + artifact.
# Everything else (docs, archive, agent configs, caches, local data) stays local.
*
!.gitignore
!README.md
!LICENSE
!build.py
!grepbot.user.js
!src/
!src/**
